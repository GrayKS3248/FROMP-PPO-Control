{Training data parameters}
total_trajectories	5000		(Total number of trajectories over which to collect training data)
samples_per_trajectory	20		(Number of temerpature and cure frames sampled per trajecotry for AE training)
samples_per_batch	100		(Number of frames used per training epoch)
actions_per_trajectory	100		(Number of random input actions performed by controller during trajectory)
path			training_data	(Path to directory in which training data is saved)